## React 19 (React Compiler) + TypeScript + TanStack (Query, Router, Table)

### Commands

- `npm run dev` — start dev server
- `npm run build` — production build
- `npm run preview` — preview production build
- `npm run typecheck` — run TypeScript
